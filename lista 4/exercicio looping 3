// Online C compiler to run C program online
#include <stdio.h>

int main() {
    int num, numero, soma = 0;
    for (num = 1; num <= 5; num++) {
        if(numero > 0){
            soma = soma +numero;
        }
        }
        printf("A soma foi de %d", numero);


    return 0;
}
